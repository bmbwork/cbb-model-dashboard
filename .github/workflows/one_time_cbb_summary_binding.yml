name: One-time CBB live summary binding
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/one_time_cbb_summary_binding.yml'
permissions:
  contents: read
jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "CBB one-time runner is healthy"
